{
  "name": "velvet-and-blood",
  "version": "0.1.0",
  "description": "Velvet and Blood – A City That Remembers",
  "type": "module",
  "scripts": {
    "compile": "node scripts/compile.js",
    "dev": "npm run compile && vite",
    "build": "npm run compile && vite build",
    "deploy": "npm run build && gh-pages -d dist",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "inkjs": "^2.3.0"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0",
    "typescript": "^6.0.3",
    "vite": "^8.0.16"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
